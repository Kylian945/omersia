# ========================================
# STOREFRONT - LOCAL (Dev sans Docker)
# ========================================
# Architecture: Browser → localhost:3000 (Next.js) → localhost:8000 (Nginx) → Backend
# - Port 3000 = Next.js dev server (public)
# - Port 8000 = Nginx reverse proxy
# - Port 8001 = Backend Laravel
# ========================================

# 🔵 SERVEUR - URLs internes (SSR/API Routes)
API_INTERNAL_URL=http://backend:8001/api/v1    # API backend direct
BACKEND_URL=http://backend:8001                 # Backend base (images, etc.)
NEXT_INTERNAL_URL=http://localhost:3000         # Next.js local

# 🟢 CLIENT - URLs publiques (Browser)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1      # API via Nginx
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000          # Backend via Nginx
NEXT_PUBLIC_FRONTEND_URL=http://localhost:8000         # Frontend

# 🔑 Authentification & Services
FRONT_API_KEY=dev_api_key_change_in_production
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key
