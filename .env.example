# ========================================
# ENVIRONNEMENT LOCAL (docker-compose.yml)
# MySQL sur l'hôte | Backend, Storefront, Meilisearch dans Docker
# ========================================
# Ce fichier est utilisé par docker-compose.yml pour substituer les variables
# Copiez ce fichier en .env et ajustez les valeurs selon votre configuration
# ========================================

APP_NAME=Omersia
APP_ENV=local
APP_KEY=base64:YOUR_APP_KEY_HERE
APP_DEBUG=true
APP_URL=http://localhost:8000

# Database MySQL
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=8889
DB_DATABASE=omersia
DB_USERNAME=root
DB_PASSWORD=root

# Meilisearch (container Docker)
MEILI_MASTER_KEY=masterKey

# API Keys
FRONT_API_KEY=dev_api_key_change_in_production
