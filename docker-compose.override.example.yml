# ========================================
# DOCKER COMPOSE OVERRIDE (optionnel)
# Copier ce fichier en docker-compose.override.yml pour activer Vite HMR
# ========================================
# Usage:
#   cp docker-compose.override.example.yml docker-compose.override.yml
#   docker compose up -d
# ========================================

services:
  backend:
    # Utiliser le script dev avec Vite HMR
    command: sh /var/www/html/docker-entrypoint-dev.sh
    ports:
      # Exposer le port Vite pour le HMR
      - "5173:5173"
