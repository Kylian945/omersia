# Docker Compose environment variables
# COPY THIS FILE: cp .env.docker.example .env.docker
# Then configure the values below for your environment

# Database Configuration
DB_DATABASE=omersia
DB_USERNAME=omersia
DB_PASSWORD=secret
DB_ROOT_PASSWORD=secret

# MeiliSearch
MEILI_MASTER_KEY=masterKey

# Application
APP_ENV=local
APP_DEBUG=true

# API Security (generated automatically by install script)
FRONT_API_KEY=temporary_key_will_be_replaced
